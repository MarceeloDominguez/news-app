import { StyleSheet } from "react-native";

export const GlobalStyle = StyleSheet.create({
  wrapIcon: {
    flexDirection: "row",
    gap: 6,
    backgroundColor: "#fff",
    padding: 4,
    borderRadius: 100,
  },
});
